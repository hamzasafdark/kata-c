#include <stdio.h>
#include <stdlib.h>

typedef struct {
	char * first,
	char * last
} person

void main() {
	char firstname;
	char lastname;

	person * newperson = (person *)calloc(sziseof(person)) 

	scanf(newperson->first)  
	printf("Hello %s %s\n", first, last); 
}
